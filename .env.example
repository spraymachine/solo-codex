NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Client-side key for Google Books volume search (Books feature). Required — unauthenticated quota is a shared global pool and gets exhausted fast. Restrict by HTTP referrer in Google Cloud Console.
NEXT_PUBLIC_GOOGLE_BOOKS_API_KEY=
